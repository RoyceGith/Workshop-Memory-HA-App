name: Workshop Memory MCP
version: "0.1.22"
slug: workshop_memory
description: MCP server for the Workshop Memory Obsidian vault
arch:
  - aarch64
  - amd64
init: false
startup: services
boot: auto
ports:
  3001/tcp: 3001
ports_description:
  3001/tcp: Workshop Memory MCP HTTP endpoint
map:
  - type: share
    read_only: false
options:
  deploy_agent_url: "http://127.0.0.1:3010"
  deploy_agent_token: null
  code_repository_path: "/share/Workshop-Memory-HA-App"

schema:
  deploy_agent_url: str
  deploy_agent_token: password
  code_repository_path: str
